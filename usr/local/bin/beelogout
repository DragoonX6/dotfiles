#!/bin/sh

NAME=$(logname)

case "$1" in
	(logout)
		sudo beesvc stop
		mpd --kill
		openbox --exit
		exit 0
		;;
	(shutdown)
		sudo beesvc stop
		mpd --kill
		sudo shutdown -h now
		exit 0
		;;
	(reboot)
		sudo beesvc stop
		mpd --kill
		sudo reboot
		exit 0
		;;
esac
