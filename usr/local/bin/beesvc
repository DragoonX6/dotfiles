#!/bin/sh

case "$1" in
	(start)
		rc-service bumblebee start
		exit 0
		;;
	(stop)
		rc-service bumblebee stop
		exit 0
		;;
	(*)
		printf 'Usage %s \n start \t --starts bumblebee daemon \n stop \t --stops bumblebee daemon\n' "$0"
		exit 1
		;;
esac
